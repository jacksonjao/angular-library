import {SanitizationStylePipe} from './sanitization-style.pipe';

describe('SanitizationStylePipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizationStylePipe();
    expect(pipe).toBeTruthy();
  });
});
